spring.application.name=meeting-room-booking-api

# database
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=postgres
spring.datasource.password=123456
spring.datasource.url=jdbc:postgresql://localhost:5432/meeting_room_booking_db
spring.datasource.type=com.zaxxer.hikari.HikariDataSource
spring.datasource.hikari.minimum-idle=10
spring.datasource.hikari.maximum-pool-size=50
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.show_sql=true
# liquibase
spring.liquibase.enabled=true

# mail (configure via environment variables — do NOT put real credentials here)
spring.mail.host=${SMTP_HOST:smtp.gmail.com}
spring.mail.port=${SMTP_PORT:587}
spring.mail.username=${SMTP_USERNAME:hung2k4love@gmail.com}
spring.mail.password=${SMTP_PASSWORD:whua rvxn fods atpi}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# booking business rules
booking.min-duration-minutes=15
booking.max-duration-hours=4
booking.workday-start-hour=7
booking.workday-end-hour=22
booking.max-advance-booking-days=30
